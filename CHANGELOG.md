# Triangle Agency 更新日志

## v2.8.0 - 2026年8月17日

### 异常能力系统增强

#### 1. 被动能力模式

- **主动/被动切换**：异常能力卡片标题栏一键切换（闪电⚡ = 主动 / 盾牌🛡 = 被动），点击即时生效并自动保存
- **被动显示**：被动能力无成功时/失败时（不掷骰），成功区变为「描述」占满整行，隐藏失败区；框体颜色不变
- **模板支持**：GM 异常能力模板同样支持被动开关，授予角色后玩家卡片即为被动样式；旧数据默认主动，完全兼容

#### 2. 第三问答（t3 / t3v / p3）

- **编辑/创建弹窗**：增加第三个问答输入位，不填则不显示在卡片上
- **卡片显示**：问题答案区支持三个答案轨道，各自带值（高墙文件编号）和计数器方点
- **右键涂抹计数器**：每个答案后的计数器支持右键涂抹（灰色斜线），可涂抹 1-2 个，卡片只显示未涂抹数量；涂抹状态持久化保存；编辑弹窗始终显示 3 个点并同步涂抹状态

#### 3. 异常能力 JSON 导入

- **导入数据**：直接从 `data/anomaly-import.json` 导入（162 个能力：119 主动 + 43 被动，含成功时/失败时、问题答案及高墙文件编号）
- **一键导入**：GM「异常」tab 创建模板按钮左侧新增「导入 JSON」按钮，批量导入能力模板

#### 4. 编辑弹窗导入（玩家端）

- **导入按钮**：异常能力编辑弹窗标题栏新增「导入」按钮（关闭按钮左侧）
- **能力选择器**：点击弹出筛选弹窗，按分组展示《异常能力手册》（anoms.json）全部能力，支持按名称实时搜索
- **一键填充**：点击能力条目自动填入名称、触发器、资质、成功时/失败时、问题、答案及编号
- **被动行对齐修复**：修复被动能力勾选框与文字错位（CSS 特异性覆盖导致），垂直居中显示；删除「（无成功时/失败时）」括号说明文字

### 角色卡打印（print-sheet.html）

- **A4 分页**：异常能力按 A4 页面高度自动分页，卡片超出一页时移入新的 A4 页面，修复内容截断与下半页无背景色问题
- **精确测量**：分页基于固定 A4 高度计算（不随内容撑高），幂等执行，字体加载完成后自动重算，打印前（beforeprint）再次重算
- **打印补全**：被动能力显示「描述」标签（不再显示成功/失败），第三问答及计数器正常渲染

### 高墙文件授权与异常能力同步修复

- **先授权后补模板**：修复「角色已授权高墙文件、之后才补录对应模板」时能力无法写入角色卡的问题——保存权限时对所有已勾选文件重新尝试补写能力（幂等：同名跳过）
- **模板自动同步**：创建/更新异常能力模板时，自动将能力补写给所有已授权对应高墙文件的角色，无需再次操作权限
- **历史数据补齐**：对现有全部授权记录执行补写，修复 5 个角色缺失的对应能力

### Bug 修复

- 修复 MVP / 察看期记录在角色更新时被清零（PUT 保留 `mvpRecords` / `watchRecords`）
- 修复进度追踪编辑按钮红框无图标、悬浮文字与格子颜色对比不清的样式问题
- 修复机构邮箱/高墙文件点击跳转改为弹窗查看（样式与收件箱邮件一致）
- 修复异常能力涂抹计数器保存无效与输入框光标问题

## v2.7.0 - 2026年7月23日

### 主要功能更新

#### 1. 办公室系统（取代广场）

- **等距 3D 工位俯视图**：纯 SVG 参数化生成办公桌/显示器/椅子/键盘，等距投影 + 深度排序实现立体遮挡，响应式自适应屏幕（手机 1-2-1-2 / 电脑 N 与 N-1 蜂窝交错）
- **角色入座**：角色按工位入座，显示头像（无头像时首字圆框）+ 名字，地砖按进度三轴（职能红/现实黄/异常蓝）自动配色
- **拼图地砖**：每块地砖不规则形状（伪随机扰动 + 确定性种子）+ 厚度侧面 + 模糊投影，深浅随机
- **排序**：MVP / 察看期 / 出勤 三种排序，点击重排工位
- **分部切换**：左下角分部下拉，切换查看不同分部工位，自动记忆
- **定位自己**：点击滚动到自己的工位并红色光晕闪烁（多角色循环定位）
- **签名**：本人可编辑签名（复用 plazaMessage 字段），他人只读，多角色弹选择列表
- **浮窗**：悬停/点击头像显示三轴属性（带色）+ MVP/察看期/出勤 + 全部模组 + 签名，浮窗配色适配角色主题色
- **入口替换**：dashboard 广场入口改为办公室，广场（plaza）页面废弃

#### 2. 角色头像上传

- **头像上传**：角色卡「欢迎你特工」板块新增头像，cropper.js 裁剪成 1:1 正方形
- **双存储**：本地模式存 `data/uploads/pcimg/{charId}.png`，COS 模式存 `pcimg` 文件夹，自动判断
- **旧头像清理**：上传新头像自动删除旧文件（本地 unlink / COS deleteObject）
- **多端显示**：角色卡 + 办公室工位均显示头像（圆形裁剪），无头像回退首字圆框

#### 3. 办公室可见性

- 原「在广场中隐藏」文案统一改为「在办公室中隐藏」（玩家端 dashboard + 管理员端 manager），字段名 `plazaHidden` 保持不变以兼容

### 技术改进

- 引入 cropper.js（本地 vendor，正方形头像裁剪）
- 新增 `POST /api/character/:id/avatar` 头像上传接口（multer + COS/本地双模式）
- 办公室列表接口 `/api/plaza/characters` 增加 `pAvatar` 字段返回
- 等距投影参数化生成器（`isoBox`/`isoWorkstation`），全局 `SCALE` 因子控制工位大小

## v2.6.0 - 2026年7月1日

### 主要功能更新

#### 1. NPC 关系预设系统（新增 NPC Tab）

- **NPC 关系模板**：经理可在管理后台「NPC」Tab 提前预设关系卡（姓名、扮演者、描述、连结加成、连结等级 0-9、是否激活）
- **一键添加关系**：角色卡片底部、任务详情特工卡片均可「添加关系」，从预设模板批量勾选赋予
- **写入关系网**：赋予的关系直接写入角色 `data.reals[]`，玩家在 sheet 关系网中即时可见

#### 2. 天气 / 事件卡系统

- **天气卡数据**：基于三角机构骰子表，44 张天气/事件卡（1-20 组，含 a/b/c 递进变体 + 散逸端/特殊卡），存储为 `data/tianqi.json`
- **GM 端天气设置**：任务面板顶部天气计数器，点击弹出全卡表，支持搜索、多选、保存并广播
- **递进展开**：选了 1c 自动补全 1a、1b（同组低位自动生效），bar 上显示完整名称链
- **bar 名称 + Tooltip**：天气栏直接显示天气名称，鼠标悬停查看每张卡的完整效果详情
- **玩家端实时同步**：Socket 广播天气变更，画板左上角悬浮天气按钮（雨伞图标 + 下雨动效），无天气时隐藏
- **天气详情弹窗**：Win98 风格窗口 + 天气 App 布局，左侧大图标（T1-T5 按数量分级）+ 右侧温度式序号 + 下方详细效果
- **故障闪烁效果**：图标带 RGB 色散 + 不规律电子闪烁（T1 最轻 → T5 最严重），无位移纯色变

#### 3. 腾讯云 COS 对象存储

- **COS 上传支持**：管理后台「系统设置」新增 COS 配置区（开关 + SecretId/Key/Bucket/Region/自定义域名）
- **智能切换**：开启 COS 后图库上传走对象存储，关闭则保持本地磁盘，动态生效无需重启
- **凭证安全**：SecretKey 与 SMTP 密码同等遮蔽处理
- **向后兼容**：历史本地图片不受影响，COS 图片删除时同步清理对象

#### 4. 角色卡打印功能

- **独立打印页**：新增 `print-sheet.html`，A4 排版的角色卡打印视图，含基本信息、异常能力、关系网、职能等完整档案
- **一键打印**：角色卡片底部打印按钮（红底白图标），点击 `printSheet()` 新窗口打开打印页
- **按钮位置**：打印按钮移至删除按钮左侧，与操作按钮统一配色

#### 5. 画板手机端触控适配

- **触控拖拽**：图片拖拽、缩放手柄适配手机端（`touch-action:none` + `setPointerCapture`），手指拖动不被页面滚动抢占
- **弹窗触控**：图片弹窗去掉触控禁用，手机端可拖动/缩放
- **连线弹窗**：NPC 关系类型选择弹窗美化（图标+描述+色点），修复手机端弹出瞬间消失问题
- **自适应高度**：横竖屏切换时画板高度自动重算

### UI / UX 优化

- **角色卡片按钮统一**：7 个操作按钮统一红色、删除改黑色、编辑保持橙红，重构为 `.btn-op` 统一类
- **打印按钮**：移到删除左边，红底白图标
- **Manager Tab**：「入职申请」缩短为「申请」，手机端 tab 图标居中，桌面端防挤压
- **天气 Tooltip**：向下弹出可滚动，bar 名称完整不省略
- **关系类型弹窗**：从纯色块改为图标+标题+描述的卡片式布局，触控友好

### 技术改进

- 新增 `cos.js` COS 工具模块（懒加载客户端、URL 构造、上传/删除）
- 新增 `routes/npc-templates.js`（CRUD + grant/revoke）
- 新增 `data/tianqi.json` 天气卡数据源
- 新增 `public/print-sheet.html` 角色卡打印页
- 数据库新增 `npc_templates` 表、`field_missions.weather` 列
- `/api/options` 接口扩展返回 `tianqi` 数据
- 新增依赖 `cos-nodejs-sdk-v5`

---

## v2.1.0 - 2026年5月6日

### 主要功能更新

#### 1. 申领物管理系统

- **申领物商店**：完整的物资申领系统，支持一次性/限制型物品
- **经理台管理**：经理可以创建、编辑、删除申领物
- **分部隔离**：每个分部拥有独立的申领物商店
- **物品分配**：经理可批量分配申领物给角色
- **物品类型**：支持基础型、一次性使用、分阶定价等多种物品类型

#### 2. Siphon商店系统

- **解锁机制**：阅读高墙文件X2后自动开放
- **申诫消耗**：使用申诫点数购买特殊能力
- **购买记录**：完整的购买历史记录
- **分部关联**：商品和购买记录按分部隔离

#### 3. UNL3ASH系统（高墙文件U2）

- **解锁按钮**：阅读U2后在异常页面显示特殊按钮
- **激活条件**：申诫达到3点时可激活

#### 4. 异常能力模板系统

- **模板管理**：经理可创建、编辑异常能力模板
- **关联高墙文件**：模板可关联具体的高墙文件
- **自动授予**：阅读关联文件时自动获得对应异常能力
- **分部隔离**：每个分部拥有独立的异常能力库
- **完整配置**：支持触发条件、成功效果、失败效果、进阶选项等

#### 5. 入职申请系统

- **用户申请**：新用户可申请加入分部
- **管理审批**：分部经理和管理员可审批申请
- **状态管理**：申请状态包括待审批、已加入、已拒绝
- **自动通知**：申请结果会通过系统消息通知
- **分部选择**：支持搜索和选择目标分部

#### 6. 分部功能完善

- **多部门隔离**：所有数据按分部隔离
- **权限管理**：
  - 超级管理员：管理全部分部
  - 经理：管理本分部用户和角色
  - 玩家：只能访问所属分部内容
- **分部统计**：用户数量、角色数量、任务统计、逸散端统计
- **可搜索选择器**：支持快速搜索和切换分部
- **入职申请**：完整的申请审批流程

#### 7. 角色卡UI重构

- **卡片化设计**：
  - 异常能力卡片
  - 关系网卡片
  - 申领物卡片
- **三栏布局**：档案页采用更清晰的三栏布局
- **侧面板优化**：侧面板浮于容器外，提供更好的视觉层次
- **响应式设计**：适配各种屏幕尺寸
- **统一图标系统**：使用Font Awesome图标库
- **交互动画**：流畅的过渡动画和交互反馈

### 技术改进

#### 后端优化

- **模块化拆分**：server.js从4250行拆分为17个模块文件
- **路由分离**：功能路由独立模块
- **数据库优化**：
  - 添加分部相关表
  - 申领物表结构优化
  - Siphon商店表
  - 异常能力模板表
  - 入职申请表

#### 前端重构

- **CSS/JS分离**：独立的CSS和JS文件
- **统一样式系统**：
  - CSS变量统一管理
  - 通用样式库
  - 组件化样式
- **性能优化**：
  - 减少DOM操作
  - 优化事件监听
  - 懒加载图片

#### 安全性增强

- **JWT认证**：完整的用户认证系统
- **权限控制**：细粒度的权限管理
- **分部隔离**：数据访问严格按分部限制
- **输入验证**：前后端数据验证

### 📱 用户体验提升

#### 导航优化

- **侧菜单系统**：统一的侧边导航菜单
- **图标按钮**：直观的图标化操作按钮
- **快速切换**：分部快速切换功能

#### 视觉设计

- **深色主题**：统一的深色背景设计
- **动画效果**：流畅的过渡动画
- **视觉反馈**：清晰的操作反馈

#### 移动端适配

- **响应式布局**：完美适配移动设备
- **触摸优化**：优化的触摸交互
- **手势支持**：滑动手势操作

### 🗂️ 数据库变更

#### 新增表

- `branches` - 分部信息
- `user_branches` - 用户分部关联
- `requisitions` - 申领物
- `siphon_products` - Siphon商店商品
- `siphon_purchases` - Siphon购买记录
- `anomaly_templates` - 异常能力模板
- `branch_applications` - 分部入职申请

#### 表结构优化

- `characters` - 添加分部关联字段
- `field_missions` - 添加分部关联字段
- `character_document_permissions` - 高墙文件权限表

### 📝 文档更新

- **README**：完整的项目文档
- **API文档**：详细的API接口说明
- **部署指南**：服务器部署教程

### 🐛 问题修复

- 修复dashboard重复initConsoleButton的bug
- 修复权限验证问题
- 修复数据隔离问题
- 优化内存泄漏问题

### 🔄 数据迁移

- 添加数据库迁移脚本
- 支持旧数据升级
- 保留历史数据完整性

### 🎨 UI/UX改进

- 统一的视觉设计语言
- 改进的信息层次结构
- 优化的色彩系统
- 提升可读性和可访问性

### 📊 统计数据

- **新增文件**：40+ 个
- **代码行数**：+7486 / -4097
- **新增功能**：7 个主要功能模块
- **数据库表**：7 个新表
- **API接口**：50+ 个新增接口

---

## 升级指南

### 从 v1.x 升级到 v2.0

1. **备份数据**：
   
   ```bash
   cp data/database.db data/database.db.backup
   ```

2. **更新代码**：
   
   ```bash
   git pull origin main
   ```

3. **安装依赖**：
   
   ```bash
   npm install
   ```

4. **数据库迁移**：
   
   - 系统会自动检测并迁移数据库结构
   - 首次启动会创建新的分部系统

5. **重启服务**：
   
   ```bash
   npm start
   ```

### 注意事项

- 升级后需要为现有用户分配分部
- 检查申领物和Siphon商店配置
- 验证权限设置是否正确

---

**更新日期**: 2026年5月6日
**版本**: v2.1.0

---

## v2.2.0 - 2026年5月10日

### 画板系统（NPC / 地图）

- **共享画板**：GM 和玩家共享任务画板，双方独立操作互不干扰
- **Socket.IO 实时同步**：经理端操作即时同步到其他经理，玩家端操作即时同步给其他玩家
- **图片素材库**：支持上传/删除/重命名图片，按 NPC 和地图分类管理
- **文件夹系统**：图片素材库支持磁盘文件夹（上传时自动创建 NPC/MAP 子目录），可新建/重命名/删除文件夹
- **拖拽摆放**：从图库拖入图片到画布，支持拖拽移动和右下角缩放（等比例）
- **图片边框**：白色卡片边框 + 红色姓名条（姓名在外）
- **置顶机制**：点击图片时自动提升到最上层
- **边界约束**：图片无法拖出画布左上角之外
- **双击弹窗**：玩家双击图片弹出可拖动浮窗查看大图，右下角可等比缩放

### 地图连线系统

- **蓝色虚线连线**：GM 点击"连线"按钮后依次点击两个图片创建连线
- **最近边锚点**：连线自动选择两个图片间最近的边作为锚点
- **连线标签**：连线备注用对应颜色的深色圆角矩形 + 白字显示
- **宽判定区**：连线下方有 14px 透明宽路径，双击和右键更容易命中
- **右键菜单**：图片右键菜单支持重命名/添加连线/删除；连线右键菜单支持删除
- **实时同步**：GM 创建/删除连线实时同步到玩家端

### 玩家 NPC 关系连线

- **四种关系类型**：友善（绿）、敌对（红）、中立（黄）、未知（紫）
- **玩家自主连线**：玩家可右键连接 NPC，选择关系类型，仅在玩家画板保存
- **双击备注**：玩家双击连线可编辑备注文字（如"情人"、"儿子"）
- **右键删除**：右键连线可删除
- **磁盘落库**：玩家 NPC 连线存入 `player_npc_connections` 表，GM 端不可见

### 任务面板

- **横排职员卡片**：每个任务成员一整行，从左到右展示姓名、资质保证（隐藏为0的属性）、基础信息、异常能力、关系、物品
- **资质保证列**：显示专注/欺瞒等属性值，标记用红色感叹号显示
- **混沌/逸散端计数器**：顶部直接调节任务数值，自动保存同步
- **面板按钮**：从任务详情一键打开独立面板页面
- **三 tab 切换**：总览 / NPC画板（统一画布含地图连线）/（发邮件）

### 骰子系统

- **骰子剪影按钮**：d4(红三角)、d6(方)、d8(菱)、d10(六边)、d12(五边)、d20(八角)、d%(方)，各有专属形状
- **检定按钮**：投 6d4 统计"3"的个数，结果中 3 标红
- **广播机制**：骰子结果通过 Socket.IO 广播给同任务所有玩家和 GM
- **历史记录**：本地存储 50 条骰子历史，可弹窗查看和清除
- **GM 端实时显示**：GM 画布左上角实时显示玩家的骰子结果
- **角色名标注**：每条骰子结果标注角色名（蓝色）

### 邮件系统增强

- **GM 群发邮件**：在任务面板点击"发邮件"，自动列出任务参与者（可勾选/取消），输入标题内容后批量发送
- **Socket.IO 实时通知**：收到新邮件时邮箱图标立即变红，角标数字实时更新
- **删除邮件**：收件箱每封邮件右上角垃圾桶按钮，点击确认后删除

### 分部选择优化

- **位置调整**：dashboard 页面分部选择器移到右侧按钮旁边
- **持久化修复**：修复闭包变量捕获 bug，切换分部后正确保存到 localStorage
- **登录保护**：登录页不再强制覆盖已保存的分部选择
- **Manager 页修复**：修复 `logout()` 误用 `localStorage.clear()` 清掉分部选择的问题
- **默认登录页**：管理员登录后默认进入 manager 页面

### 其他改进

- **弹窗缩放修复**：角色卡编辑弹窗高度改用 `dvh`，`flex-start` 避免底部被地址栏遮挡
- **关系编辑优化**：连结进度圆点和加减按钮缩小适配手机端
- **画板按钮颜色**：底部 bar 的"画板"按钮激活时显示紫色
- **右键菜单美化**：统一红色三角箭头 + 白色背景设计，图标与边框间距增大
- **移动端 dice 按钮**：手机端只显示检定和历史，其他骰子整合在浮窗中

### 数据库变更

- `image_library` — 图片素材库（folder 字段）
- `mission_boards` — 任务画板
- `board_images` — 画板图片（经理/玩家坐标分离）
- `board_connections` — 地图节点连线
- `player_npc_connections` — 玩家 NPC 连线

### 新增 API

- `/api/image-library/*` — 图片素材库 CRUD + 文件夹管理
- `/api/board/:missionId/*` — 画板图片和连线 CRUD
- `/api/board/:missionId/npc-connection*` — 玩家 NPC 连线 CRUD
- `/api/manager/send-mail` — GM 批量群发邮件
- `/api/character/:id/message/:msgId` DELETE — 删除邮件

### 新增依赖

- `socket.io` — WebSocket 实时通信
- `multer` — 文件上传处理

---

## v2.3.0 - 2026年5月12日

### 桌面端底部栏重构

- **移除 nav-bar**：桌面端（≥768px）隐藏底部导航栏，改用页面内圆形按钮
- **资质保证下方**：档案/画板开关（绿色/紫色滑块切换）
- **基础信息下方**：异常（老式电脑图标）、关系（电话图标）、物品（公文包图标）三个圆形按钮
- **tab 顺序重排**：画板移到第一位（默认首页），档案第二位
- **tab 记忆恢复**：`localStorage('ta_sheet_tab')` 保存最后查看的 tab，下次打开直接跳转

### 异常 / 关系 / 物品弹窗

- **异常 Win98 窗口**：经典 Windows 98 风格 SVG 边框，噪点滤镜 + 故障抖动动画，标题栏最小化/最大化/关闭按钮 + U2蓝色眼睛按钮，点击标题"异常能力.exe"添加异常
- **关系手机**：SVG 手机边框容器，电话图标 + 计数 + 添加按钮，可拖动
- **物品公文包**：SVG 公文包边框（深棕色皮质+金色搭扣），公文包图标 + 计数 + 添加按钮，可拖动

### 弹窗打开动画

- **异常**：噪点闪烁（SVG `feDisplacementMap` 滤镜层）+ 从中间水平线上下展开（`scaleY` 动画）
- **关系**：从下方弹性弹出（`translateY(100vh)` → `translateY(0)`，0.45s）
- **物品**：弹性缩放弹出（`scale 0.85 → 1.02 → 1`，0.4s）

### 公文包纸张散射特效

- **红三角纸片**：16 张奶油色纸片（左右各 8 张），每张印有红色倒三角标记
- **散射动画**：从公文包背后向两侧飞散，独立延迟、旋转和位移
- **独立散射层**：临时 `paper-scatter-overlay`（z-index 3990，低于公文包 4000），动画结束自动删除
- **CSS 变量驱动**：`--dx`/`--dy`/`--dr`/`--dx2`/`--dy2`/`--dr2` 六变量控制每张纸的位移与旋转

### 窗口置顶系统

- **统一 z-index 管理**：三窗口共用全局计数器 `window._popupZ`
- **打开置顶**：每次打开弹窗自动升至最上层
- **点击置顶**：弹窗内任意区域 pointerdown 即置顶
- **拖动置顶**：拖动标题栏时自动置顶

### bug 修复

- 修复画板双击浮窗失效（`bringToFront` 在 `pointerdown` 中提前移动 DOM 导致 `dblclick` 事件丢失）
- 修复画板拖动时选中文本（补回 `e.preventDefault()`）
- 修复桌面端画板底部空白（画布改用 `flex:1` 填满容器，桌面端缩减 `padding-bottom`）
- 修复公文包散射层位置错乱（`offsetLeft/Top` 改用 `style.left/top`，纸张初始位置向公文包边缘校准）
- 修复三窗口关闭后无法重开（inline `display:none` 与 CSS class `display:block` 冲突，打开时先清空 inline）
- 修复 `.desktop-hide` 类缺失导致桌面端 swiper 显示空白 tab
- 修复 tabOrder 与 DOM 顺序不匹配导致 swiper 滚动位置错误

## v2.4.0 - 2026年5月12日

### 弹窗动画精细调优

- **纸张散射定位**：纸张以公文包实际位置为基准偏移，左右两侧竖排整齐贴合边框
- **飞行距离优化**：纸张散射距离减半（dx 50~110），从公文包背后自然飞散
- **右侧纸张校准**：右列 ix 从 515 收至 500，避免溢出遮挡

### 文档更新

- CHANGELOG 补充 v2.3.0 和 v2.4.0 版本记录

## v2.5.0 - 2026年5月26日

### 基础架构重构

- **sqlite3 → better-sqlite3**：数据库驱动从异步 `sqlite3` 替换为同步 `better-sqlite3`，`utils.js` 中 5 个函数改为 `db.prepare().get()/all()/run()` 同步 API
- **ESLint + Prettier**：代码规范工具链搭建（ESLint v10 + Prettier）
- **nodemon**：开发环境自动重启
- **Vite 开发服务器**：开发端口 30303，代理到 Express 3333，生产环境 Express 直接服务 `public/`

### 前端模块化拆分

- **sheet.js（3584行）→ 20 个 ES module**：`public/js/sheet/` 下按功能拆分，可变状态封装到 `S` 对象
- **manager.js（2900+行）→ 12 个 ES module**：`public/js/manager/` 下按功能拆分，可变状态封装到 `S` 对象
- **共享模块提取**：`public/js/common-modules/`（auth.js, ui.js, nav.js, index.js）
- **HTML 切换 `type="module"`**：sheet.html 和 manager.html 引用 `init.js` 入口
- **全局函数挂载**：HTML `onclick` 需要的函数通过 `window.xxx = xxx` 挂载，状态变量用 `Object.defineProperty` 代理

### 破坏条功能（新增）

- **数据库**：新增 `destruction_tracks` 表（branch_id + cell_index）
- **后端 API**：`routes/destruction-tracks.js`（GET/PUT 格子状态、GET has-access+branchId）
- **经理台**：新增"破坏条" tab（炸弹图标），42 格蛇形排列（7×6），蓝色边框，点击切换激活
- **角色卡**：邮箱左侧眼睛按钮（需解锁 L10/Q3/X3 权限才显示），点击打开全屏弹窗（复用 full-page-modal 动画）
- **蛇形排列修复**：修复 `track-snake` class 干扰导致格子顺序错乱（14→34 等乱序），改用独立的 `dest-snake` class

### UI/UX 改进

- **Favicon**：红色三角形 SVG favicon.svg，所有 HTML 页面已添加
- **浏览器返回黑屏修复**：`common.js` 添加 `pageshow` 事件监听，bfcache 恢复时移除 overlay active 类
- **加入分部转场动画**：申请弹窗红色主题动画，按钮状态（白底红字/红底白字申请中）
- **角色卡返回转场**：`goBack()` 使用 transition-overlay 动画
- **经理台 tab 优化**：桌面端 tab 文字精简（角色/任务/申领物/Siphon/异常/破坏条），手机端只显示图标一排排列
- **破坏条样式统一**：手机端直接复用桌面端样式，正方形格子（aspect-ratio: 1），gap 12px

### Bug 修复

- 修复 `documents.js` PUT 路由中 `currentRows` 变量缺失
- 修复 `board.js` 对象 key 误替换、`init.js` 局部变量误替换
- 修复 `mail.js` 缺少 `addScatteringRow`/`addObjectiveRow` 导出
- 修复 utils.js better-sqlite3 API 转换（`db.get()` → `db.prepare().get()`）
- 修复破坏条 `track-snake` CSS 冲突导致格子乱序
